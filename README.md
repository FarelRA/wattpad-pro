# Wattpad Pro

Tailwind-styled Wattpad reader page (single-file, self-contained).

## Development

```bash
bun install
bun run build   # tailwindcss -> output.css (minified)
bun run dev     # watch mode
```

`drafts/` holds earlier iterations of the page.
